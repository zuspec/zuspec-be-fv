# zuspec-be-fv

Formal verification backend for Zuspec dataclasses.

## Overview

This package provides tools for formally verifying properties of Zuspec dataclass definitions using SMT solvers. It enables automated checking of bounds constraints, invariants, and other properties at design time.

## Installation

```bash
pip install -e .
```

## Quick Start

(Coming soon - API under development)

## Development

Install development dependencies:

```bash
pip install -e ".[dev]"
```

Run tests:

```bash
pytest
```

## Status

🚧 Under active development - Phase 1 implementation in progress
